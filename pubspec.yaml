name: share_localisation
description: This plugin shared localisation data between platforms.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.5.4

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.11

